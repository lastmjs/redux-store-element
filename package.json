{
  "name": "redux-store-element",
  "version": "0.4.1",
  "description": "A custom element allowing a more declarative use of Redux.",
  "keywords": [
    "web-components"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/lastmjs/redux-store-element.git"
  },
  "dependencies": {
    "redux": "^3.6.0"
  },
  "devDependencies": {
    "zwitterion": "git+ssh://git@github.com/lastmjs/zwitterion.git"
  },
  "scripts": {
    "start": "node dev-init && npm run build && zwitterion --serve-dir example --write-files-off",
    "build": "zwitterion --build-static"
  }
}
