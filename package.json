{
  "name": "redux-store-element",
  "version": "1.2.4",
  "description": "A custom element allowing a more declarative use of Redux.",
  "keywords": [
    "web-components"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/lastmjs/redux-store-element.git"
  },
  "dependencies": {
    "redux": "^3.6.0"
  },
  "devDependencies": {
    "deep-equal": "^1.0.1",
    "jsverify": "^0.8.2",
    "tape": "^4.6.3",
    "scram-engine": "^0.6.1"
  },
  "scripts": {
    "test": "npm run link && electron --enable-logging node_modules/scram-engine/main.js --entry-file test/index.html --auto-run",
    "test-window": "npm run link && electron --enable-logging node_modules/scram-engine/main.js --entry-file test/index.html --window",
    "link": "npm link && npm link redux-store-element"
  }
}
