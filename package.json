{
  "name": "redux-store-element",
  "version": "0.2.4",
  "description": "A simple web component written with the Polymer library that allows a more declarative use of Redux.",
  "keywords": [
    "web-components"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/lastmjs/redux-store-element.git"
  },
  "dependencies": {
    "redux": "^3.5.2"
  },
  "devDependencies": {
    "es-no-build": "^0.1.0",
    "http-server": "^0.9.0",
    "systemjs-builder": "^0.15.26"
  },
  "scripts": {
    "http-server": "http-server",
    "build": "node production-builder.js",
    "start": "node dev-init && npm run build && npm run http-server"
  }
}
