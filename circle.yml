machine:
    node:
        version: 8.0.0

dependencies:
  override:
    - npm install # install all npm dependencies
    - npm install --only=dev # install all npm devDependencies
    - npm install -g bower # install bower globally so that we can install our bower packages
    - bower install # install our bower packages (this will go away once Polymer is supported on npm)
